#property indicator_chart_window
#property indicator_buffers 1
#property indicator_plots 1

double SignalBuffer[];

int OnInit()
{
   SetIndexBuffer(0, SignalBuffer, INDICATOR_DATA);
   PlotIndexSetString(0, PLOT_LABEL, "{{ indicator_name }}");
   return(INIT_SUCCEEDED);
}

int OnCalculate(const int rates_total,
                const int prev_calculated,
                const datetime &time[],
                const double &open[],
                const double &high[],
                const double &low[],
                const double &close[],
                const long &tick_volume[],
                const long &volume[],
                const int &spread[])
{
   // TODO: inject validated indicator calculations
   return(rates_total);
}
