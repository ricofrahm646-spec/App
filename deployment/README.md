# Deployment

## Local stack

```bash
docker compose up --build
```

## Dashboard autostart

Use `deployment/autostart_dashboard.sh` from OS startup mechanisms (systemd user service, cron `@reboot`, or desktop autostart entry).
